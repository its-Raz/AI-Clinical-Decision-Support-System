# Purpose: Configuration settings used across agents
# Contains:
#
# Model names (GPT-4, embedding model)
# API keys loading (from .env)
# Iteration limits (MAX_REACT_ITERATIONS, MAX_REFLEXION_ITERATIONS)
# Timeouts
# Cost tracking settings
#
# Why separate: Avoids duplicating config in each agent file